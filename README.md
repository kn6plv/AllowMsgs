AREDN LAN Message Firewall
===

An AREDN LAN based firewall filter which restricts the use of the AREDN LAN network to only texting/messating apps.
Current it allows:

* WhatsApp
* iMessage
* Google RCS
* Signal

Only texts are allowed. Larger media (e.g. photos, videos, etc.) are blocked.
